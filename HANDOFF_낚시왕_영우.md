# 낚시왕 영우 작업 인수인계 문서

## 2026-06-02 최신 작업 메모

### 현재 작업 폴더

```text
C:\Users\PC\Documents\Codex\2026-06-01
```

### 오늘 반영/확정 요약

- 칭호 획득 알림은 공통 `EVENT` 테두리를 사용한다.
- 영우의 일반 대사는 이벤트가 아니므로 `EVENT` 테두리를 제거한다.
- 상점 이벤트 확인 버튼은 홈/필드와 같은 하단 확인 구조로 맞춘다.
- 영우 portrait/이벤트 컷인은 하단 프레임에 붙고 좌우 기준 중앙 정렬을 우선한다.
- 낚시대 미장착 토글 동작을 복구했다.
- `뚜벅이` 메인화면은 와이프 압수 이벤트 확인 이후에 뜬다.
- 제1레져 파손 후 영우 대사는 스포를 줄여 `상점에 환불하러 가보자` 방향으로 정리했다.
- 재모 이벤트는 홈이 아니라 민물 필드에서, 오리발 획득 창 확인 후 발생한다.
- 민물보트 획득 후 이미지가 뜨는데 미획득 장비로 처리되던 상태를 보정했다.
- 상점 사장 대사는 고급차 구매 고객에게 태도가 달라져 서비스해주는 아부성 문구로 변경했다.
- 바다 필드 카니발/티볼리 위치와 크기는 G70 기준과 맞춘다.

### 영우 얼굴 이모지 최신 기준

- 기존 감정 시트를 잘라 쓰는 방식은 흔들림이 많아, 얼굴 프레임용 이모지는 **126x130 개별 투명 PNG**로 제작하는 방향이 좋다.
- 게임 내 얼굴 프레임 기준:
  - 크기: `126x130`
  - 배경: 실제 RGBA 투명 PNG
  - 저장 위치: `assets/youngwoo_sprites/portrait_fixed/`
- 닮음 기준:
  - 사진의 파란 햇빛가리개/후드, 얇은 안경, 작은 눈, 낚시 현장 느낌을 우선한다.
  - 영우는 잘생기거나 순한 얼굴이 아니다.
  - 기본 인상은 작은 눈, 안경, 평범한 얼굴, 약간 킹받는 무표정이다.
  - 얼굴이 너무 그늘지면 게임에서 읽히지 않으므로 v7처럼 밝게 보정한다.
- 가장 중요한 최신 방향:
  - v7: 얼굴 밝기와 기본 닮음 기준.
  - v8: 표정이 전부 울상처럼 보이지 않도록 눈썹을 수평에 가깝게 잡는 기준.
  - v9: 영우 특유의 킹받는 인상 기준. 작은 눈, 삐딱한 눈썹, 무심한 입을 살린다.

### 생성된 영우 얼굴 프레임 샘플

사용 가능성이 높은 최신 파일:

- `assets/youngwoo_sprites/portrait_fixed/youngwoo_beginner_neutral_fishing_photo_v7_bright_face_126x130.png`
- `assets/youngwoo_sprites/portrait_fixed/youngwoo_beginner_neutral_fishing_photo_v8_nonsad_126x130.png`
- `assets/youngwoo_sprites/portrait_fixed/youngwoo_beginner_happy_fishing_photo_v8_nonsad_126x130.png`
- `assets/youngwoo_sprites/portrait_fixed/youngwoo_beginner_surprised_fishing_photo_v8_nonsad_126x130.png`
- `assets/youngwoo_sprites/portrait_fixed/youngwoo_beginner_annoyed_fishing_photo_v8_nonsad_126x130.png`
- `assets/youngwoo_sprites/portrait_fixed/youngwoo_beginner_confused_fishing_photo_v8_nonsad_126x130.png`
- `assets/youngwoo_sprites/portrait_fixed/youngwoo_beginner_determined_fishing_photo_v8_nonsad_126x130.png`
- `assets/youngwoo_sprites/portrait_fixed/youngwoo_beginner_smug_kingbat_v9_126x130.png`
- `assets/youngwoo_sprites/portrait_fixed/youngwoo_beginner_sideeye_kingbat_v9_126x130.png`
- `assets/youngwoo_sprites/portrait_fixed/youngwoo_beginner_unimpressed_kingbat_v9_126x130.png`

프리뷰:

- `assets/youngwoo_sprites/portrait_fixed/youngwoo_fishing_photo_v6b_vs_v7_bright_compare.png`
- `assets/youngwoo_sprites/portrait_fixed/youngwoo_fishing_photo_v8_nonsad_expression_preview.png`
- `assets/youngwoo_sprites/portrait_fixed/youngwoo_kingbat_v9_preview.png`

### 다음 작업 추천

1. v9 킹받는 얼굴 중 하나를 기본 영우 neutral 기준으로 확정한다.
2. 확정 얼굴을 기준으로 24표정 전체를 `126x130` 개별 PNG로 만든다.
3. `game_home.html`의 portrait 렌더링을 감정 시트 crop 방식에서 개별 PNG 로딩 방식으로 바꾼다.
4. 홈/필드/상점/도감 얼굴 프레임에서 같은 파일 체계를 공유한다.

---

## 2026-06-01 최신 작업 메모

### 현재 작업 폴더

```text
C:\Users\PC\Documents\Codex\2026-05-31\1-rpg-1-40-2-10
```

### 오늘 최종 반영 요약

- 게임 흐름은 `민물 -> 첫 바다 실패 -> 민물보트 -> 바다 재도전 -> 바다보트 -> 대양/진엔딩 준비` 구조로 정리했다.
- 공통 하단 대사창 구조는 홈/필드/상점/도감에 적용되어 있으며, 호칭 장착 시 영우 이름 앞에 호칭이 붙는다.
- 도감 목표는 `어종 60 + 유실물 20 + 메인화면 3 + 칭호 5 = 88` 기준이다.
- 일반엔딩은 어종 60종 완료, 진엔딩은 유실물/메인화면/칭호까지 전부 수집하는 조건이다.
- 관리자 메뉴는 일반 플레이에서 숨김 처리했다.
  - 홈: `game_home.html?dev=1`일 때만 `관리자` 버튼 표시.
  - 필드 디버그 URL도 `dev=1`이 있어야 작동.

### 현재 가격/장비 밸런스

- 낚시세트 전투 능력:
  - 장난감: 명중 -8, 감기력 8, 안정성 5
  - 알리산: 명중 0, 감기력 17, 안정성 14
  - 일본산: 명중 7, 감기력 30, 안정성 25
  - 제1레져: 명중 0, 감기력 19, 안정성 7
  - 테무산: 명중 4, 감기력 28, 안정성 18
  - 전설: 명중 11, 감기력 50, 안정성 38
- 현재 상점 가격:
  - 알리산 2,800G, 일본산 7,500G, 제1레져 3,000G
  - 테무산 11,000G, 전설 30,000G
  - G70 4,500G, 카니발 14,000G, 바다보트 20,000G
  - 중급 낚시복 1,800G, 고급 낚시복 11,000G
- 의상 표시 능력치는 `희귀어 출현`으로 통일했다.
  - 내부적으로는 유실물 보정도 남아 있지만, 상점 표시 문구에는 유실물 능력치를 직접 노출하지 않는다.

### 호칭/칭호/엔딩

- 호칭 5종:
  - `뚜벅이`: 보유 차량 0대일 때 획득, 자동 장착.
  - `장비병`: 장비 올수집.
  - `부자`: 누적 수입 100,000G.
  - `유투버`: 누적 명성 20,000.
  - `낚시왕`: 일반엔딩 달성.
- `부자`는 현재 보유 골드가 아니라 누적 수입 기준으로 변경했다.
- `유투버`는 현재 명성이 아니라 누적 명성 20,000 기준으로 변경했다.
- 진엔딩 조건은 실제 도감의 `칭호 5종` 전체 보유 여부와 일치시켰다.
- 일반엔딩 이후 홈 목표 대사는 남은 수집 현황을 보여준다.
  - 예: `유실물 7/20 · 메인화면 2/3 · 칭호 2/5`

### 유실물/필수 수집품

- 유실물은 총 20종이다.
- 필드 화면에는 유실물이 기본 1개 보장된다.
  - 중급/고급 낚시복 또는 뚜벅이 보정이 있을 때만 낮은 확률로 2개가 보인다.
  - 보정 없음: 1개 고정.
  - 중급 낚시복: 2개 약 14%.
  - 고급 낚시복: 2개 약 29%.
  - 고급 + 뚜벅이: 2개 약 43%.
- `아들의 성적표`는 일반엔딩 이후에만 출현한다.
- `준우가 훔친 G70 열쇠`는 조건 충족 후 백상아리 첫 포획 시 확정 획득으로 변경했다.
  - 기존 20% 반복 운빨은 폐기.
- 영구효과는 실제 전투에 반영된다.
  - `영우의 갯바위화`: 최대 텐션 +50.
  - `와이프의 카드영수증`: 감기력 +3.

### 낚시 조작/손맛 개선

- 필드 화면의 깨진 한글 로그를 정상 문구로 교체했다.
- 던지기/챔질/힘겨루기 대사를 손맛 있게 보강했다.
- 입질 시 물결/반짝임 피드백을 추가했다.
- 힘겨루기 중 상태 문구를 추가했다.
  - `손맛 올라온다`
  - `줄이 비명을 지른다`
  - `거의 다 왔다`
  - `놓으면 빠진다`
- 보스 체감 조정:
  - 대물 쏘가리/백상아리는 현재 장비 기준에서 어렵지만 가능.
  - 돗돔은 전설 장비의 감기력이 초고저항 때문에 너무 과하게 깎이지 않도록 보정했다.
  - 갯바위화/카드영수증이 있으면 최종 보스전 안정성이 확실히 좋아진다.

### 어종/도감

- 어종은 60종 기준이다.
  - 민물 30종, 바다 30종.
- 지역명 `풍천`, `북방`은 제거했다.
  - `풍천 뱀장어` -> `뱀장어`
  - `북방 참다랑어` -> `참다랑어`
- 도감은 민물/바다 6x5 그리드 기준으로 한 화면에 보이게 구성했다.
- 미획득 어종은 필드 출현 가중치를 높였다.
- 필드 후보 어종도 장비 성능 기준을 반영한다.
  - 테무산으로 바다보트 진입 시 대양 후반 일부는 제외.
  - 전설 장비면 백상아리/돗돔까지 후보에 포함.

### 이벤트/상점 흐름

- 제1레져는 바다 첫 시도에서 파손된다.
- 카니발 구매 후 상점주인이 부러진 제1레져를 새 제1레져로 교환해주는 흐름이다.
- 테무산은 제1레져 파손 상태에서는 구매 불가 대사를 보여준다.
- 바다보트는 카니발/제1레져 교환 이후 바다 재진입 이벤트를 본 뒤 상점에서 살 수 있다.
- G70 차키는 백상아리 포획으로 확정 회수된다.

### 오늘 검증한 것

- `game_state.js` 문법 검사 통과.
- `fishing_screen.html`, `game_home.html`, `shop_screen.html`, `fishdex_screen.html` 인라인 스크립트 검사 통과.
- 브라우저에서 홈/상점/도감/필드 화면 로딩 확인.
- 콘솔 에러 없음 확인.
- 필드에서 `던지기` 클릭 후 새 한글 대사 출력 확인.
- 관리자 버튼:
  - 일반 모드 숨김 확인.
  - `game_home.html?dev=1`에서 표시 확인.
- 엔딩/칭호 동시 조건 충족 시 큐 순서 확인:
  - 장비병 -> 부자 -> 유투버 -> 일반엔딩 -> 진엔딩.

### 집에서 이어서 할 일 추천

1. 실제 플레이 루트 QA.
   - 새 게임 기준으로 민물 돈벌이부터 진엔딩 준비까지 직접 한 번 이어서 확인.
2. 필드 손맛 추가 강화.
   - 사운드, 진동 느낌의 화면 흔들림, 보스 입질 전용 연출을 넣으면 체감이 더 좋아진다.
3. 유실물/어종 에셋 보강.
   - 현재 일부 유실물/어종은 임시 이미지나 기존 이미지 재사용 가능성이 있다.
4. 어종별 이동 패턴 차등화.
   - 작은 어종은 빠르게, 대형 어종은 느리지만 묵직하게.
5. 엔딩 이미지/팝업 최종 다듬기.
   - 일반엔딩/진엔딩 팝업 배경과 문구 최종 톤 점검.
6. 개발용 파일 정리.
   - `work` 폴더, preview HTML, 오래된 handoff 내용 정리 여부 결정.

---

## 2026-05-31 최신 작업 메모

### 현재 작업 폴더

```text
C:\Users\Kruz\Documents\Codex\2026-05-30\1-rpg-1-40-2-10
```

### 오늘 확정된 큰 방향

- 메인화면은 진행도별 3종 이미지로 운영한다.
  - 1번 기본: 초현대식 아파트 단지 입구에서 티볼리, 장난감 낚시세트, 입문복을 착용한 영우.
  - 2번 민물보트 획득 후: 민물보트에서 알리산 낚시세트와 중급복으로 대형 민물고기와 힘겨루기.
  - 3번 바다보트 획득 후: 기존 바다/대어 메인 이미지.
- 도감의 기존 `이벤트` 탭은 제거하고, 그 자리를 `메인화면` 탭으로 대체한다.
- 엔딩은 별도 전체화면 이동보다, 인트로씬 느낌을 유지한 팝업형 컷씬으로 보여주는 방향이 좋다.
- 관리자 메뉴에서는 이벤트 미리보기 버튼을 전부 빼고, 팝업형 엔딩 미리보기만 남긴다.

### 새로 추가/교체된 메인화면 에셋

- `assets/main_stage_start_apartment.png`
  - 초현대식 아파트 단지 입구 배경.
  - 영우 왼쪽 눈 어색함 수정.
  - 장난감 낚시대는 `rod_toy_icon.png`, `tackle_toyRod_with_knob.png` 기준으로 조정.
  - 릴은 노란 원형 릴 + 빨간 별모양 장식.
  - 추는 노란 원형 추 + 작은 보라색 구슬.
  - 낚시대 본체와 파란 손잡이는 두께를 줄여 최종 반영.
- `assets/main_stage_valley_boat.png`
  - 사용자가 마음에 든다고 확정한 2번 메인화면 에셋.
  - 물고기는 특정 종 고정이 아니라 “대형 민물고기” 연출.
  - 설정상 붙이면 대형 송어/산천어 계열이 자연스럽다.
- `assets/main_hero_youngwoo_yellowtail_v5.png`
  - 3번 메인화면으로 유지.

### `index.html`

- `.screen` 배경을 `--main-bg` CSS 변수로 바꾸었다.
- 저장 상태에 따라 메인 배경을 선택한다.
  - `sea_boat_owned`가 있으면 3번.
  - 아니고 `valley_boat_owned`가 있으면 2번.
  - 둘 다 아니면 1번.
- 최근 문법 검사: `index.html: ok`.

### `fishdex_screen.html`

- 도감 UI를 무리하게 상단 제거/하단 현황형으로 바꾸다가 레이아웃이 깨져서, 현재는 원래 도감 배치로 되돌리는 방향으로 복구했다.
  - 상단 HUD 4칸, 좌측 탭/목록, 우측 상세, 하단 `아들과의 약속` 진행바 + 나가기 버튼 구조를 유지한다.
  - `이벤트` 탭 자리는 `메인화면 3종` 탭으로 사용한다.
  - `엔딩` 탭은 `엔딩 5종` 기준으로 기획한다.
  - `유실물`은 20종 기준으로 확장 예정이며, `game_state.js`에는 임시 5종을 추가해 현재 20종으로 맞춰 두었다.
  - 목록 이미지는 제거하고, 이미지는 우측 상세 패널에서만 표시하는 방향이 좋다.
  - 단, 미획득 항목은 우측 상세 이미지도 표시하지 않는 것이 사용자 요구다.
- 메인화면 탭 획득 조건:
  - 1번 기본 메인화면은 게임 기본 실행 시점부터 획득 상태로 보여야 한다.
  - 2번은 `valley_boat_owned`, 3번은 `sea_boat_owned` 기준으로 획득 처리한다.
- 다음 세션에서 확인할 것:
  - `fishdex_screen.html` 실제 브라우저 화면에서 원래 배치가 완전히 복구됐는지.
  - 한글 깨짐이 남아 있는 탭/상세/현황 문구.
  - 메인화면 3종 이미지가 우측 상세에서만 뜨는지.
  - 미획득 어종/메인화면/엔딩의 우측 상세 이미지가 비어 있는지.

### `game_state.js`

- `junkData`는 `유실물` 명칭으로 통일한다.
- 기존 15종에서 20종으로 늘릴 예정이라, 임시 5종을 추가했다.
  - `민수의 방수팩`
  - `재모의 오리발 한짝`
  - `준우의 자동차 키링`
  - `정훈이의 캠핑컵`
  - `혜진이의 선크림`
- 낚시복 능력치에는 `유실물 습득` 보너스 개념을 둔다.

### `shop_screen.html`

- 보트 상세 능력치에 세 번째 줄 추가:
  - `신규 메인화면 획득`
- 차량 3대 상세 능력치에 세 번째 줄 추가:
  - `바다 필드 진입 가능: 가능`
- 장난감 낚시대에 잘못 섞였던 `신규 메인화면 획득` 줄은 제거했다.
- 최근 문법 검사: `shop_screen.html: ok`.

### 제1레져 추 에셋

- `assets/tackle_firstLeisure.png`를 사용자가 제시한 기준 이미지에 맞춰 다시 제작했다.
  - 줄/낚싯줄은 보이지 않아야 한다.
  - 세로로 선 독립 추 형태.
  - 금색 상단 링/캡, 파란 중앙 패널, 은색 V형 측면 패널.
  - 기존 허접한 수작업 도형 버전은 폐기하고, 이미지 생성본에서 배경 제거 후 투명 PNG로 저장했다.
- `fishing_screen.html`의 `tackle_firstLeisure` 참조 버전은 캐시 갱신용 쿼리를 붙여 둔 상태다.

### 이벤트/엔딩/화자 상태

- 와이프 얼굴 에셋은 티볼리 압수/부시리 이벤트에서 영우 사진 대신 표시되어야 한다.
- 화자 이름은 사진 기준으로 동기화해야 한다.
  - 영우 사진이면 `남영우`, 차량 0개면 `뚜벅이 남영우`, 40종 엔딩 이후는 `낚시왕 남영우`.
  - 와이프 사진이면 `와이프`.
- G70 관련 설정은 `상점에서 새 차를 구매했지만 친구 준우가 훔쳐서 달아났다`로 확정한다.
- 티볼리 압수 후 대사는 바다에 다시 가려면 돈을 모아 새 차를 뽑아야 한다는 방향이다.
- 엔딩은 전체 페이지 이동보다 창고 내부에서 인트로씬 느낌의 큰 팝업으로 보여주는 쪽이 현재 의도다.

### 오늘 이야기한 민물고기 20종 후보

```text
붕어, 잉어, 향어, 메기, 가물치, 쏘가리, 꺽지, 피라미, 갈겨니, 모래무지,
누치, 끄리, 배스, 블루길, 송어, 산천어, 은어, 장어, 참붕어, 황쏘가리
```

### 다음에 이어서 할 일

- `game_home.html`
  - 엔딩 팝업 CSS 일부는 추가된 상태다.
  - 아직 HTML/JS 연결은 마무리 필요.
  - `routePendingEnding()`이 `ending_scene.html`로 이동하지 않고 창고 내 팝업을 열게 변경해야 한다.
  - 관리자 메뉴는 이벤트 버튼을 제거하고 엔딩 버튼 4개만 팝업 미리보기로 연결해야 한다.
- `fishdex_screen.html`
  - `이벤트` 탭 제거/메인화면 탭 대체 작업 검수.
  - 메인화면 3종 이미지 썸네일 UI 정리.
- `game_home.html`
  - 와이프 얼굴 에셋 적용이 완전히 안정화됐는지 재확인 필요.
- 파일 인코딩/깨진 한글:
  - 일부 HTML은 PowerShell 출력에서 한글이 깨져 보이지만 브라우저에서는 정상인 부분이 있다.
  - 실제 화면 기준으로 확인하면서 필요한 곳만 조심해서 수정할 것.

---
## 1. 작업 폴더

현재 작업물 위치:

```text
C:\Users\Kruz\Documents\Codex\2026-05-26\1-rpg-1-40-2-10
```

새 계정 또는 새 Codex 세션에서 위 폴더를 열고 이어서 작업하면 된다.

## 2. 주요 파일

```text
낚시왕_영우_GDD.md
index.html
game_home.html
fishing_screen.html
field_variants.html
shop_screen.html
fishdex_screen.html
event_scene.html
prototype_app.html
game_state.js
assets/player_sheet.png
assets/youngwoo_outfit_mid.png
assets/youngwoo_outfit_premium.png
assets/tivoli_sprite_sheet.png
assets/tivoli_left.png
assets/tivoli_right.png
assets/g70_sprite_sheet.png
assets/g70_left.png
assets/g70_right.png
assets/carnival_sprite_sheet.png
assets/carnival_left.png
assets/carnival_right.png
assets/valley_boat_sprite_sheet.png
assets/valley_boat_left.png
assets/valley_boat_right.png
assets/sea_boat_sprite_sheet.png
assets/sea_boat_left.png
assets/sea_boat_right.png
HANDOFF_낚시왕_영우.md
```

## 3. 게임 콘셉트 요약

`낚시왕 영우`는 주인공 영우가 아들과의 약속을 지키기 위해 40종의 물고기를 모두 낚는 레트로 도트풍 병맛 시트콤 낚시 RPG다.

핵심 루프:

```text
낚시 -> 골드 획득 -> 장비/차량/보트 구매 -> 필드 해금 -> 도감 수집 -> 이벤트 진행 -> 엔딩
```

핵심 톤:

- 병맛 개그
- 가족 약속
- 억까와 배신
- 장비 성장
- 수집형 도감
- 이벤트 플래그 기반 진행

## 4. 현재까지 만든 화면 목업

### 공통 화면 스케일링

모든 HTML 목업은 브라우저 크기에 따라 내부 오브젝트를 재배치하지 않고, 1180x664 기준 게임 화면 전체를 통째로 스케일링한다.

공통 규칙:

- 모든 HTML 화면은 `.game-frame` 래퍼 안에 `.screen`을 넣는 구조
- `.game-frame`은 `width: calc(1180px * var(--game-scale))`, `height: calc(664px * var(--game-scale))`로 실제 표시 크기를 잡음
- `.screen`은 `width: 1180px`, `height: 664px` 고정
- `--game-scale` CSS 변수로 전체 화면 확대/축소
- `.screen`은 `transform: scale(var(--game-scale))`, `transform-origin: top left`
- `resizeGame()`에서 현재 브라우저 크기에 맞춰 스케일 계산
- `resizeGame()`은 `window.visualViewport`가 있으면 그 값을 우선 사용한다.
  - 모바일 브라우저 주소창/하단바가 나타나거나 사라져도 실제 보이는 영역 기준으로 스케일을 다시 계산하기 위함
  - `resize`, `orientationchange`, `visualViewport.resize` 이벤트에 모두 연결됨
- 게임 화면은 일반 반응형 웹처럼 줄바꿈/재배치하지 않음
- 브라우저 크기 변경 시 화면이 오른쪽으로 밀리던 문제는 `.game-frame` 래퍼 추가로 해결됨
- 모바일 세로 화면은 낚시 화면의 회전 안내 오버레이로 대응

### 공통 상태 저장

파일:

```text
game_state.js
```

현재 역할:

- 모든 실제 프로토타입 화면이 공유할 상태 저장소
- `localStorage` 키: `fishingKingYoungwooState`
- 기본 상태:
  - 골드 0
  - 명성 0
  - 민물 장비: 장난감 세트
  - 바다 장비: 없음
  - 차량: 티볼리
  - 보트: 없음
  - 기본 플래그: `intro_complete`
- 제공 함수:
  - `GameState.load()`
  - `GameState.save(state)`
  - `GameState.reset()`
  - `GameState.hasItem(state, type, itemId)`
  - `GameState.addItem(state, type, itemId)`
  - `GameState.removeItem(state, type, itemId)`
  - `GameState.setLoadout(state, type, itemId)`
  - `GameState.setFlag(state, flag)`
  - `GameState.triggerTivoliConfiscation(state)`
  - `GameState.queueEvent(state, eventId)`
  - `GameState.peekEvent(state)`
  - `GameState.consumeEvent(state, eventId)`

상태 정규화:

- 저장/불러오기 시 보유 목록과 장착 슬롯을 정규화한다.
- 의상은 반드시 1개 착용해야 하며, 저장 상태가 꼬여도 기본 의상 `beginnerOutfit`으로 복구된다.
- 티볼리 압수 상태에서는 티볼리가 차고와 인벤토리에 남지 않는다.
- 부시리 이벤트 후 `tivoli_returned`가 켜지면 티볼리가 다시 보유 목록에 들어온다.
- G70 먹튀 상태에서는 G70이 제거되고, 백상아리 보조키 이벤트 후 다시 복구된다.
- 제1레져 파괴 후에는 제1레져가 제거되고 테무산 낚시세트가 지급된다.
- 저장된 장착 장비가 더 이상 보유 중이 아니면 안전한 기본 장비로 되돌린다.
- 기존 잡템 6종은 삭제되었고, 저장 상태에 남아 있어도 현재 `junkData`에 없는 잡템 이름은 로드 시 제거된다.

이벤트 큐:

- `pendingEvents`: 아직 팝업으로 보여주지 않은 이벤트 ID 목록
- `seenEvents`: 이미 확인한 이벤트 ID 목록
- `endings`: 수집한 엔딩 ID 목록
- 주요 이벤트는 발생 시 즉시 상태를 바꾸고 `pendingEvents`에 이벤트를 넣는다.
- 거점 화면은 현재 표시 중인 이벤트 ID를 넘겨 해당 이벤트만 소비한다.
  - 여러 이벤트가 동시에 쌓였을 때 팝업에 보인 이벤트와 실제 소비 이벤트가 어긋나는 문제를 막기 위함
- 엔딩도 같은 팝업 큐로 보여주며, 확인 후에도 게임은 계속된다.

현재 연결된 화면:

- `game_home.html`
  - 장착 관리가 `game_state.js`의 보유/장착 상태를 읽음
  - 장착 변경 시 `localStorage`에 저장
  - 티볼리 압수 등 주요 이벤트를 공통 팝업으로 표시하고 확인 후 다음 대사로 갱신
- `prototype_app.html`
  - 테스트 액션으로 얻은 G70, 제1레져, 테무산 등이 공통 저장소에 반영됨
  - 저장/불러오기/초기화가 공통 저장소 기준으로 동작
- `shop_screen.html`
  - 골드/보유 여부/플래그를 읽어 구매 가능 상태 표시
  - 구매 시 골드 차감, 보유 목록 추가, 관련 플래그 저장
- `fishing_screen.html`
  - 테스트 포획 버튼으로 포획/잡템 결과를 공통 저장소에 기록
  - 골드/명성/도감/잡템 컬렉션 상태 갱신
- `fishdex_screen.html`
  - 공통 저장소의 `caughtFish`, `junk`, `seenEvents`, `endings`, `fame`을 읽어 획득/미획득 상태와 진행률 표시
- `field_variants.html`
  - 공통 저장소의 장착/보유/플래그 상태를 읽어 필드 입장 가능 여부 표시
  - 첫 바다 진입 시도 시 티볼리 압수 플래그와 차량 미장착 상태 저장
  - 현재 플레이 데모 루프에서는 홈 화면에서 낚시 화면으로 바로 이동하므로 필드 비교/디버그용 화면에 가깝다.
- `index.html`
  - `새 게임 시작`은 공통 저장 상태를 초기화하고 거점으로 이동
  - `이어하기`는 저장 상태를 유지하고 거점으로 이동

### 메인 화면

파일:

```text
index.html
```

현재 구성:

- 메인화면: `낚시왕 영우`
- 부제: `낚시왕이 되기 위한 험난한 여정`
- 버튼:
  - `새 게임 시작`
  - `이어하기`
- 왕관 이미지는 제거됨
- `새 게임 시작` 버튼은 `game_home.html`로 연결되어 있음
- `새 게임 시작` 버튼은 `game_state.js` 상태를 초기화한 뒤 `game_home.html`로 이동
- `이어하기` 버튼은 기존 저장 상태를 유지한 채 `game_home.html`로 이동

### 거점/차고 화면

파일:

```text
game_home.html
```

현재 구성:

- 차고/보관소 실내 화면
- 현재 수집 장비와 차량이 진열되는 공간
- 장비 6종 고정 슬롯
- 차량 3종 고정 슬롯
  - 티볼리
  - G70
  - 카니발
- 홈 화면 창고의 차량 3종은 정면 전시 에셋을 사용한다.
  - 티볼리: `assets/tivoli_front.png`
  - G70: `assets/g70_front.png`
  - 카니발: `assets/carnival_front.png`
- 필드/상점 등 기존 측면 연출이 필요한 화면에서는 기존 오른쪽 방향 에셋을 계속 사용할 수 있다.
- 보트 2종 보관 슬롯
  - 밸리보트
  - 바다보트
- 밸리보트는 신규 세미픽셀 보트 에셋 `assets/valley_boat_right.png`를 차고 슬롯에 연결함
- 좌우 전환용 밸리보트 에셋은 `assets/valley_boat_left.png`, `assets/valley_boat_right.png`로 분리되어 있으며, 앞부분이 오른쪽으로 가는 파일은 `valley_boat_right.png`
- 바다보트는 신규 세미픽셀 보트 에셋 `assets/sea_boat_right.png`를 차고 슬롯에 연결함
- 좌우 전환용 바다보트 에셋은 `assets/sea_boat_left.png`, `assets/sea_boat_right.png`로 분리되어 있으며, 앞부분이 오른쪽으로 가는 파일은 `sea_boat_right.png`
- 보트는 차고에 보관 슬롯은 표시하되, 장착 상태는 `없음` / `밸리보트` / `바다보트` 중 선택하는 온/오프 방식
- 우측 메뉴:
  - `민물 낚시`
  - `바다 낚시`
  - `상점`
  - `장비`
  - `도감`
- `장비` 버튼을 누르면 차고 내부에 `장착 관리` 패널이 열림
- 장착 관리 패널:
  - `민물 장비`
  - `바다 장비`
  - `차량`
  - `보트`
  - `의상`
- 보유중인 항목은 장착 가능, 미보유 항목은 잠김 상태로 표시
- 장착 변경 시 상단 HUD와 차고 라벨, 로그박스 문구가 갱신됨
- 보트 탭에는 `없음` 항목이 있어 보트 미장착으로 되돌릴 수 있음
- 의상은 홈 화면 창고 의상칸에서 장착 가능하며, 반드시 1개는 착용해야 하므로 장착중 의상을 다시 눌러도 벗지 않음
- 장착 상태는 `game_state.js`를 통해 저장되므로 화면을 다시 열어도 유지됨
- 상단 HUD:
  - 민물 장비
  - 바다 장비
  - 장착 차량
  - 장착 보트
  - 골드
  - 명성
- 하단 로그박스:
  - 이벤트 플래그에 따라 다음 목표 힌트가 표시되는 구조
  - 예: 시작 직후에는 민물에서 돈을 모으라는 힌트

사용자 피드백 반영 사항:

- 민물 필드처럼 보이면 어색해서 차고 실내로 변경
- 로그박스 portrait는 낚시 화면과 같은 스타일을 유지해야 함
- portrait 박스는 로그 프레임 안에서 위/아래 여백이 같아야 함
- 상단 도감 수치와 `아들과의 약속` 진행도는 중복이므로 상단 도감 수치는 제거됨

### 낚시 화면

파일:

```text
fishing_screen.html
```

현재 구성:

- 현재 낚시 화면은 이전 작업물 `C:/Users/Kruz/Desktop/new-chat/game.js`의 정상 동작 낚시 방식을 참고해 캔버스 기반 루프로 재구성됨
- 민물/바다 낚시 화면
- 영우가 물가 발판 위에 서 있음
- 발판은 좌측 끝까지 연결
- 차량은 육지 배경에 표시
- 밸리보트와 바다보트는 거점/차고 화면에 보관 슬롯으로 표시
- 물고기는 수면 위가 아니라 물속 실루엣처럼 보이게 조정
- 하단 UI:
  - 영우 portrait
  - 로그박스
  - 우측 하단 원버튼 액션 패널
- 필드 상단 HUD는 플레이 영역을 덜 가리도록 축소형으로 유지
- 필드 우측 상단 `차고` 버튼으로 거점 복귀 가능
- 홈 화면의 `이벤트` 메뉴는 제거됨
  - 이벤트 연출은 메뉴에서 따로 보는 화면이 아니라 실제 이벤트 발생 시 팝업/연출 템플릿처럼 호출되는 방향
- 상단 HUD:
  - 필드
  - 장비
  - 명성
  - 골드
- 캐스팅/후킹/파이팅 게이지는 화면 중앙 캔버스 위에 상태별로 나타났다가 사라짐
- 캐스팅바의 녹색/노란색/빨간색 구간은 고정되고 흰 마커만 움직임
- 캐스팅 확정 후 찌와 낚싯줄은 직선 이동이 아니라 포물선 곡선으로 날아감
- 거리/좌우 조준 중에는 노란 착수 타겟 링과 예상 궤적을 표시해 특정 물고기를 노릴 수 있게 함
- 물고기는 중앙에 뭉치지 않도록 좌우/깊이별 밴드에 분산 배치
- 물고기는 잡기 전에는 수중 실루엣으로만 표현
- 영우 발판은 물고기보다 위 레이어로 그려 발판 위에 물고기가 올라온 것처럼 보이지 않게 처리
- 포획 성공 시 영우가 잡은 물고기/잡템을 들어 올리고, 이름/골드/레벨 또는 종류/설명이 담긴 결과 팝업을 표시
- 실제 구현에서는 캐스팅 게이지와 파이팅 게이지를 동시에 표시하지 않고 상태에 따라 전환
- 캐스팅은 2단계 구조:
  - 1단계: 가까움/멀리 거리 조준
  - 2단계: 좌/우 방향 조준
  - 방향 오차와 거리 오차를 합산해 착수 정확도를 계산
  - 현재 데모에서는 입력값을 기준으로 찌 착수 위치를 정하고, 가장 가까운 수중 대상과의 거리로 후킹 가능도를 계산함
  - 버튼 입력 순간 값이 확정되고, 후킹 후에는 버튼을 누르고 떼며 텐션/포획 진행도를 조절함
- Lv.10 보스급 어종 등장 알림 배너 추가
  - 예시 문구: `수면 아래에서 말도 안 되는 그림자가 움직인다...`
  - 조작을 멈추지 않고 1~2초만 표시하는 방향
- `game_state.js`와 연결됨
- 공통 콘텐츠 데이터도 `game_state.js`에 있음:
  - `fieldData`: 민물 기본, 민물 밸리보트, 바다 근해, 대양 보트
  - `gearStats`: 장비별 정확도/파워/안정성 보정
  - `fishData`: 40종 어종 데이터
  - `junkData`: 잡템 15종
  - `selectCatchResult(state, accuracy)`: 필드/장비/정확도 기반 결과 선택
- 하단 버튼은 단계형 낚시 미니게임 목업으로 동작:
  - 캐스팅 시작
  - 방향 조준
  - 거리 조준
  - 착수 판정
  - 챔질
  - 릴 감기
  - 결과/실패
- 결과 성공 시 현재 필드, 장착 장비, 캐스팅 정확도에 따라 어종/잡템을 선택하고 공통 저장 상태에 기록
- Lv.10 어종 포획 시 보스급 어종 알림 배너가 짧게 표시됨
- 대양 보스급 어종은 전설의 낚시세트 장착 + 높은 캐스팅 정확도 조건에서 후보에 들어온다.
  - 이전에는 저항력 필터 때문에 백상아리/크라켄이 사실상 후보에서 빠질 수 있었으므로, 전설 장비 조건에서 별도 보스 도전 예외를 둠
- 실패 시 줄 끊김 로그만 표시하고 도감/보상은 기록하지 않음
- 포획 결과는 골드/명성/도감/잡템 컬렉션에 반영됨
- 제1레져 낚시세트를 장착한 상태로 바다/대양에서 최초 캐스팅하면 즉시 파괴 이벤트가 발생
  - 제1레져 제거
  - 테무산 낚시세트 지급 및 자동 장착
  - `first_leisure_first_cast`, `first_leisure_broken`, `temu_rod_owned` 플래그 저장
- 제1레져 파괴 후 바다/대양 낚시 화면에서 `차고` 버튼을 눌러 나가려 할 때 G70 먹튀 이벤트가 자동 발생
  - G70 제거
  - 차량 미장착 상태로 변경
  - `g70_stolen` 플래그 저장
- G70 먹튀 이후 민물 기본 필드에서 첫 성공 결과는 `재모의 잃어버린 오리발` 인양 이벤트로 처리
  - 밸리보트 지급
  - `jaemo_flipper_found`, `valley_boat_owned` 플래그 저장
- 부시리 최초 포획 시 티볼리 반환 이벤트가 저장됨
- 백상아리 포획 시 10% 확률로 G70 보조키 획득 및 G70 재획득 처리

### 필드 4종 비교 목업

파일:

```text
field_variants.html
```

현재 구성:

- 거점/차고 화면의 `민물 낚시`, `바다 낚시` 버튼과 임시 연결됨
- `game_state.js`와 연결되어 실제 장착/보유/이벤트 플래그 상태를 읽음
- 하나의 화면에서 4개 필드를 버튼으로 전환
  - `민물 기본`
  - `민물 밸리보트`
  - `바다 근해`
  - `대양 보트`
- 각 필드 전환 시 변경되는 요소:
  - 배경 색과 수면 시작 위치
  - 발판 표시 여부
  - 차량 표시 여부
  - 보트 표시 여부
  - 영우 위치
  - 물고기 실루엣 크기
  - HUD 필드명/필요 조건/해금 어종/보트 상태
  - 하단 로그 문구
  - 대양 보트 필드의 Lv.10 보스 알림

필드별 의도:

- 민물 기본: 초반 발판 낚시, 차량은 육지 배경에 표시, 보트 미사용
- 민물 밸리보트: 재모 이벤트 후 민물 확장 어종용, 영우가 보트 위에 탑승
- 바다 근해: 차량과 바다 장비 필요, 제1레져 파괴/준우 먹튀 주요 이벤트 필드
- 대양 보트: 최고급 바다보트 후반 필드, 큰 실루엣과 Lv.10 경고 중심

현재 입장 조건 연결:

- 민물 기본: 항상 입장 가능
- 민물 밸리보트: 밸리보트 보유 필요, 실제 입장은 밸리보트 장착 필요
- 바다 근해:
  - 티볼리 보유 상태의 첫 바다 진입 시도는 바다장비 유무와 상관없이 티볼리 압수 이벤트 발생
  - 이후에는 유효 차량과 바다 장비가 모두 필요
- 대양 보트: 바다보트 보유 필요, 실제 입장은 바다보트 장착 필요
- 대양 보트도 유효 차량과 바다 장비가 필요
- 입장 실패 시 하단 로그에 부족 조건 표시
- 입장 성공 시 하단 로그에 낚시 미니게임 연결 준비 메시지 표시
- 거점의 `민물 낚시` 버튼은 `field_variants.html?field=fresh`로 이동
- 거점의 `민물 낚시` 버튼은 필드 선택 없이 바로 `fishing_screen.html`로 이동
- 거점의 `바다 낚시` 버튼은 조건 검사 후 바로 `fishing_screen.html`로 이동
- 첫 바다 진입 시도는 홈 화면에서 바로 티볼리 압수 이벤트를 발생시키고, 바다장비/차량이 부족하면 하단 대사창에 이유를 표시한다.

모바일 방향:

- 화면 터치 유지: 조준/감기
- 터치 떼기: 캐스팅/텐션 완화
- 모바일에서는 전체화면 및 가로모드 잠금 시도
- 웹 브라우저 제한 때문에 가로모드 강제는 실패할 수 있으므로 회전 안내 오버레이 필요

### 상점 화면

파일:

```text
shop_screen.html
```

현재 구성:

- 상점명: `제1레져`
- `초기 상태` / `잠적 상태` 토글 버튼으로 두 상점 상태를 확인 가능
- `game_state.js`의 공통 저장 상태와 연결됨
- 상점 탭:
  - 민물 장비
  - 바다 장비
  - 차량
  - 보트
  - 의상
- 상품 목록
- 상품 상세 패널
- 상품 클릭 시 상세 패널 갱신
- 구매 가능/불가능 상태
- 하단 구매 버튼과 나가기 버튼
- 구매 시 실제 공통 저장 상태에 반영:
  - 골드 차감
  - 보유 장비/차량/보트/의상 추가
  - 관련 이벤트 플래그 저장
  - 구매한 장비/차량/보트/의상은 즉시 장착 가능
  - 이미 보유한 의상은 상점에서 다시 선택해 장착 가능

탭별 현재 결정:

- 민물 장비 탭:
  - `장난감 낚시세트`도 보유중 장비로 표시
  - 보유중 장비는 구매 불가
  - `알리산 낚시세트`, `일본산 낚시세트` 표시
  - 알리산은 500G 이상 보유 시 구매 가능
  - 일본산은 알리산 보유 후 구매 가능
  - 바다 장비인 `제1레져 낚시세트`는 민물 장비 탭에 표시하지 않음
- 바다 장비 탭:
  - `제1레져 낚시세트` 표시
  - 제1레져는 2,000G 이상 보유 시 구매 가능
  - 제1레져 파괴 플래그 이후에는 판매 중지 표시
  - `테무산 낚시세트`는 획득 전까지 `??? 낚시세트`로 표시
  - `??? 낚시세트` 옆에는 `이벤트` 같은 스포성 단어를 쓰지 않고 `비매품`으로 표시
  - `전설의 낚시세트` 조건 표시는 `테무산 획득 필요`가 아니라 `Lv.2 필요`
  - 전설의 낚시세트는 테무산 보유 후 5,000G 이상일 때 구매 가능
- 차량 탭:
  - `익숙한 티볼리`는 구매 상품이 아니라 상태확인용으로 표시
  - 티볼리 상태 문구는 이후 `보유중` / `압수됨` / `반환됨`으로 확장 가능
  - `신차 G70` 설명: `바다에 다시 가려면 큰맘 먹고 뽑아야 하는 새 스포츠 세단.`
  - 준우는 영우의 친구 포지션이며, G70을 판매한 사람이 아니라 구매 이후 훔쳐 달아나는 인물이다.
  - G70은 실제 게임에서 티볼리 압수 이벤트 후에만 구매 가능
  - 조건 미달 시에는 `티볼리 압수 후` 같은 스포 문구 대신 `조건 미충족`으로 표시
  - `드림카 카니발`도 `G70 먹튀 후` 같은 스포 문구를 직접 표시하지 않음
  - 카니발은 G70 먹튀 플래그 이후 3,000G 이상일 때 구매 가능
- 보트 탭:
  - `재모의 밸리보트`는 비매품/이벤트 보상으로 표시
  - `최고급 바다보트`는 10,000G 상품으로 표시
  - 조건 미달 시에는 구체 이벤트명 대신 `조건 미충족`으로 표시
  - 최고급 바다보트는 카니발 플래그 이후 10,000G 이상일 때 구매 가능

상점주인 관련 결정:

- 초기 복선 대사:

```text
제1레져 낚시세트는 정말 튼튼합니다.
```

- 제1레져 장비 파괴 이벤트 후:
  - 상점주인 스프라이트 사라짐
  - 말풍선: `사장님 잠적`
  - 카운터에 종이: `교환/환불 불가`
  - 제1레져 낚시세트 판매 중지

현재 목업은 잠적 상태 예시가 기본으로 적용되어 있으며, 토글로 초기 상태도 확인 가능.

### 도감 화면

파일:

```text
fishdex_screen.html
```

현재 구성:

- 거점/차고 화면의 `도감` 버튼과 연결됨
- `game_state.js`와 연결되어 실제 수집 상태를 읽음
- 상단 HUD:
  - 전체 수집 `2 / 40`
  - 잡템/이벤트/엔딩 컬렉션 진행도
  - 명성
- 탭:
  - `민물 20종`
  - `바다 20종`
  - `잡템 15종`
  - `이벤트`
  - `엔딩`
- 좌측 도감 그리드:
  - 획득 어종은 이름과 정보 표시
  - 미획득 어종은 `???`와 잠김 실루엣으로 표시
  - 잡템은 이름을 숨기지 않고 수집/미수집 상태를 표시
  - 이벤트와 엔딩은 수집 전에는 잠김 처리하고, 수집 후 이름/설명 표시
- 우측 상세 패널:
  - 아이콘
  - 이름
  - 병맛 도감 설명
  - 분류
  - 레벨
  - HP / 저항
  - 보상
- 하단:
  - `아들과의 약속` 진행률
  - 나가기 버튼

도감 현재 예시 상태:

- 초기 상태에서는 전체 미획득/미수집
- 낚시 화면의 `테스트 포획 저장` 또는 프로토타입 테스트 액션으로 저장된 결과가 반영됨
- `caughtFish`에 있는 어종은 획득 표시
- `junk`에 있는 잡템은 수집됨 표시
- `seenEvents`/`pendingEvents`에 있는 이벤트는 이벤트 도감에 수집 표시
- `endings`에 있는 엔딩은 엔딩 도감에 수집 표시

### 이벤트 연출 템플릿

파일:

```text
event_scene.html
```

현재 구성:

- 실제 플레이 화면에서는 이벤트 메뉴를 노출하지 않는다.
- 이벤트가 발생하면 거점 화면 위에 팝업형 공통 연출로 표시한다.
- `event_scene.html`은 연출 비교와 컷씬 설계용 참고 화면으로만 남긴다.
- 참고 화면의 좌측 이벤트 선택 탭:
  - 와이프 티볼리 압수
  - 제1레져 파괴
  - 준우 문자
  - 아들 전화
  - 재모 오리발
  - Lv.10 등장
- 참고 화면의 중앙 연출 영역:
  - 컷씬 배경
  - 캐릭터 스탠딩
  - 차량 소품
  - 전화/문자 UI
  - `뚝.` 같은 컷인
- 참고 화면의 하단 대화창:
  - 화자
  - 2줄 대사
  - 나가기 버튼

현재 목적:

- 실제 컷씬 구현 전, 이벤트별 연출 형식을 비교하기 위한 템플릿
- 실제 이벤트 흐름은 `game_state.js`의 이벤트 큐와 `game_home.html`의 공통 팝업이 담당
- 준우는 친구 포지션이므로 문자 톤은 악역보다 얄밉고 장난스러운 방향 유지

### 플레이 프로토타입 뼈대

파일:

```text
prototype_app.html
```

현재 목적:

- 실제 플레이 구현 전 상태/이벤트 플래그 흐름을 테스트하는 화면
- 메인 화면의 `이어하기` 버튼과 임시 연결됨

현재 기능:

- 공통 1180x664 스케일링 적용
- 상태 표시:
  - 골드
  - 명성
  - 도감 수집 수
  - 현재 위치
  - 민물 장비
  - 바다 장비
  - 차량
  - 보트
  - 잡은 어종
  - 다음 목표
- 테스트 액션:
  - 민물 낚시 1회
  - 바다 진입 시도
  - 테스트 골드 +500G
  - G70 구매
  - 제1레져 세트 구매
  - 바다 최초 캐스팅
  - 거점 복귀
  - 저장
  - 불러오기
  - 초기화
- `localStorage` 저장/불러오기 사용
- 이벤트 플래그 표시:
  - `intro_complete`
  - `tivoli_confiscated`
  - `g70_purchased`
  - `first_leisure_owned`
  - `first_leisure_broken`
  - `temu_rod_owned`
  - `g70_stolen`
  - `jaemo_flipper_found`
  - `valley_boat_owned`

현재 프로토타입은 정식 플레이 화면이 아니라, 상태 저장과 이벤트 순서를 검증하기 위한 뼈대다.

상점 UI 스포일러 방지 원칙:

- 실제 내부 조건과 플레이어에게 보이는 조건 문구를 분리한다.
- 아직 플레이어가 겪지 않은 사건명은 상품 조건에 직접 쓰지 않는다.
- `티볼리 압수 후`, `G70 먹튀 후`, `테무산 획득 필요` 같은 문구는 UI에서 숨긴다.
- 조건을 모르는 단계에서는 `조건 미충족`, 장비 단계 조건은 `Lv.2 필요`처럼 중립적으로 표시한다.

## 5. GDD 주요 결정사항

파일:

```text
낚시왕_영우_GDD.md
```

### 장비 분리

- 민물 장비와 바다 장비는 별도 장착
- 여기서 장비는 낚싯대 단품이 아니라 낚싯대와 릴을 포함한 낚시 장비 세트
- 상단 HUD에도 민물 장비 / 바다 장비를 따로 표시

### 이동 수단

- 차량과 보트는 별도 장착
- 상단 HUD에 장착 차량 / 장착 보트 표시
- 바다 필드 진입 조건:
  - 바다 장비 보유
  - 유효 차량 보유
- 둘 중 하나라도 없으면 바다 필드 진입 차단
- 단, 티볼리 보유 상태에서 첫 바다 진입을 시도하면 바다 장비 유무와 상관없이 와이프 압수 이벤트가 우선 발생

### 차량 이벤트

티볼리:

- 초기 보유
- 첫 바다 진입 시도 시 와이프가 압수
- 일반 루트로는 재획득 불가
- 신규 이벤트 추가:
  - 대양 어종 `부시리` 최초 포획 시, 부시리를 좋아하는 와이프가 감동하여 티볼리를 돌려줌
  - 이벤트 플래그: `tivoli_returned`

G70:

- 준우는 영우의 친구이며, 완전한 악역이 아니라 얄밉고 뻔뻔한 친구 포지션
- G70은 상점에서 새 차로 구매
- 제1레져 파괴 후 바다 필드에서 `차고` 버튼으로 나가려 할 때 준우가 먹튀
- 일반 루트로는 재획득 불가
- 백상아리 포획 시 일정 확률로 G70 보조키 획득
- G70 재획득 가능

카니발:

- 구매 후 바다 필드 영구 개방

최종적으로 차량 3대 전부 보유 가능한 경우의 수가 있음:

```text
티볼리 + G70 + 카니발
```

### 재모 오리발 이벤트

- 재모 이벤트 발생 전까지 민물 필드에 `재모의 잃어버린 오리발`이 둥둥 떠다님
- 영우가 오리발을 직접 낚아야 이벤트 발생
- 1회성 이벤트
- 이벤트 후 재모가 밸리보트 지급
- 민물 확장 어종 해금
- 필요 에셋에 `재모의 잃어버린 오리발` 추가됨

### 제1레져 파괴 이벤트

- 제1레져 낚시세트는 바다 진입 전에도 구매 가능
- 바다에서 최초 캐스팅 시 확정 파괴
- 이후 낚시고수가 테무산 낚시세트를 지급
- 파괴 후 제1레져 상점주인 잠적 연출
- 파괴 후 상점에는 `사장님 잠적` 말풍선과 `교환/환불 불가` 종이를 표시

### 추가 재미요소

- 이벤트 트리거 표가 GDD에 추가됨
- 도감 10/20/30종 달성 시 아들의 중간 전화로 목표를 다시 상기
- 티볼리 압수 후 와이프 현실 대사가 거점 로그에 가끔 등장
- G70 먹튀 후 준우의 장난스러운 친구 문자 연출 해금
- 제1레져 파괴 시 `뚝.` 같은 짧은 정적 컷인 권장
- 꽝 아이템 교체:
  - 기존 민물/바다 잡템 6종은 삭제
  - 신규 공통 잡템 15종: 가인이의 뿔테안경, 익준이의 크롬하츠, 성욱이의 결혼반지, 승환이의 전자담배, 준호의 연애소설, 하빈이의 유니폼, 종연이의 세차타월, 재승이의 주식백과, 미래의 레인부츠, 기영이의 차량장부, 성범이의 마운자로, 정환이의 야구티켓, 상규의 암벽신발, 영우의 복숭아, 은지의 맛집 리스트
- 꽝 아이템은 도감 완성 조건에는 포함하지 않고 별도 잡템 컬렉션으로 기록
- 잡템 컬렉션 완성 보상 칭호: `분실물 감정사`

## 6. 로그박스 힌트 구조

거점 화면 로그박스는 현재 이벤트 플래그에 따라 다음 목표를 암시한다.

예시:

```text
시작 직후:
아들의 장난감 낚시세트 하나로 시작한다.
일단 민물에서 돈을 모으자.
바다는... 차도 장비도 있어야 갈 수 있다.

티볼리 압수 후:
차가 없다... 다시 돈을 모아야 한다.

G70 먹튀 후:
민물로 돌아가자. 뭔가 떠다니는 것 같다.
준우 이 자식 복수할테다!

재모 이벤트 후:
밸리보트가 생겼다. 더 깊은 민물로 나갈 수 있다.

카니발 구매 후:
이제 바다로 갈 수 있다. 이번엔 아무도 차를 못 가져가겠지?
```

## 7. 사용자 UI 취향 및 주의사항

중요:

- 화면은 레트로 도트풍
- 아트 기준은 `assets/player_sheet.png`의 영우 기본 캐릭터
- 이미지 생성 샘플처럼 회화적인 고퀄 배경은 과함
- 필드/차량/보트/물고기 에셋은 영우와 같은 두꺼운 외곽선, 단순 명암, 읽기 쉬운 실루엣을 우선
- 고퀄 에셋을 만들더라도 "정교한 일러스트"보다 "정리된 픽셀/세미픽셀 게임 에셋" 수준으로 제한
- 두꺼운 테두리 선호
- UI는 실제 플레이 필드를 가리지 않아야 함
- 하단 로그박스는 RPG 대화창 느낌
- portrait는 낚시 화면과 거점 화면에서 같은 느낌을 유지
- portrait 네모 박스는 로그 프레임 안에서 위/아래 여백이 같아야 함
- 정보 중복을 싫어함
  - 예: 상단 도감 수치와 `아들과의 약속` 진행도 중복 제거
- 메인 화면은 단순하게:
  - 새 게임 시작
  - 이어하기
- 설정은 메인 화면에 필요 없다고 판단
- 도감은 메인이 아니라 거점 화면에서 접근

## 8. 다음 작업 순서 제안

현재 거점 장비 장착, 상점, 도감, 공통 저장 상태, 이벤트 큐, 거점 이벤트 팝업, 낚시 화면 기본 플레이 루프까지 진행됨.

최근 검토/수정 완료:

- 모든 HTML 목업의 화면 스케일 계산이 `visualViewport`를 우선 사용하도록 보강됨
- 거점 이벤트 팝업은 현재 표시 중인 이벤트 ID를 기준으로 소비하도록 수정됨
- 대양 Lv.10 보스급 어종이 전설의 낚시세트 + 높은 정확도에서 실제 후보로 등장하도록 수정됨
- 스크립트 문법 검증 완료
- 전설의 낚시세트 + 고급 낚시복 + 정확도 100 기준 5,000회 샘플링에서 보스급 어종 등장 확인

현재 목표:

- 데모 화면 묶음이 아니라, 처음부터 끝까지 진행 가능한 출시 기반 웹게임으로 정리
- 고퀄 회화풍 에셋은 제외하고, 영우 기본 캐릭터에 맞는 도트/세미픽셀 에셋으로 제한
- 현재 상태는 `index.html`에서 시작해 저장/상점/장착/낚시/도감/핵심 이벤트가 이어지는 게임 뼈대다.
- 필드 이동 선택 화면은 실제 플레이 루프에서 제외하고 디버그/비교 화면으로 남겨둔다.
- 밸런스 수치 조정은 사용자가 직접 플레이 테스트를 한 뒤 마지막 단계에서 진행한다.

다음 순서 추천:

1. 화면 이름과 진입 흐름 정식화
   - `` 파일명을 실제 게임 화면 구조에 맞게 정리
   - 메인 -> 거점 -> 상점/장비/도감/낚시 -> 거점 루프를 기준 흐름으로 고정
   - 디버그/비교용 화면은 별도 폴더나 명확한 개발용 링크로 분리

2. 이벤트 자동 호출 흐름 확장
   - 티볼리 압수는 거점 공통 팝업으로 연결됨
   - 제1레져 파괴, G70 먹튀, 재모 오리발, 부시리 티볼리 반환, 백상아리 G70 보조키도 같은 공통 팝업 규칙으로 통일
   - 컷씬을 본 뒤 같은 이벤트가 반복되지 않도록 `pendingEvents`/`seenEvents` 기준으로 정리
   - 이벤트가 동시에 쌓인 상황에서 팝업 순서와 소비 순서가 맞는지 실제 플레이로 추가 확인

3. 도감/엔딩/진행 목표 연결
   - 40종 도감 진행도
   - 아들과의 약속 진행도
   - 엔딩 조건
   - 거점 로그의 다음 목표 안내

4. 1회차 완주 테스트
   - 새 게임 시작부터 카니발/바다보트/40종 수집까지 직접 진행
   - 이벤트 꼬임, 저장 복구, 화면 이동, 도감 반영을 우선 확인
   - 테스트용 화면과 버튼을 정식 빌드에서 숨길지 결정

5. 밸런스 조정
   - 골드 수급, 장비 가격, 파이팅 시간, 후킹 성공률은 마지막에 조정
   - 사용자가 실제 플레이 감각을 본 뒤 수치 조절

### 7일 개발 순서

| 일차 | 목표 |
|---:|---|
| 1일차 | 필드별 어종/보상 테이블, 장비 보정치, 낚시 결과 로직 |
| 2일차 | 필드 4종 실제 연결, 필드별 난이도와 조건 반영 |
| 3일차 | 상점/장비/차량/보트 성장 흐름 정리 |
| 4일차 | 티볼리 압수, 제1레져 파괴, 준우 문자, 재모 오리발, Lv.10 알림 연결 |
| 5일차 | 도감 40종, 아들과의 약속 진행도, 엔딩 조건 연결 |
| 6일차 | 영우 기준 에셋 정리, UI 다듬기, 테스트 버튼 정리 |
| 7일차 | 처음부터 엔딩까지 완주 테스트, 버그 수정, 보상 속도 조정 |

### 2026-05-28 작업 요약

오늘 반영된 핵심 변경:

1. 잡템 전체 교체
   - 기존 잡템 6종 삭제
   - 신규 공통 잡템 15종으로 교체
   - 새 잡템은 민물/바다 공통 출현
   - 골드/명성 보상 적용
   - 실물선물 증정 항목은 `rewardLabel`로 결과/도감에 표시
   - `fishdex_screen.html`은 잡템 목록을 직접 중복 관리하지 않고 `game_state.js`의 `junkData`를 읽음

2. 화면 중앙 정렬 수정
   - 모든 HTML 화면에 `.game-frame` 래퍼 추가
   - `.screen` transform 기준을 `top left`로 통일
   - 브라우저 크기 변경 시 게임 화면이 중앙에서 밀리던 문제 해결

3. 홈 화면 의상 장착 연결
   - 거점 HUD에 `장착 의상` 추가
   - 차고에 의상 슬롯 3개 추가: 입문/중급/고급 낚시복
   - 상점에서 구매한 의상을 홈 화면에서 장착 가능
   - 의상은 반드시 1개 착용해야 하므로 장착중 의상을 다시 눌러도 벗지 않음
   - 홈 영우 전신, portrait, 이벤트 영우 컷인이 장착 의상을 반영

4. 차량/보트 신규 에셋 제작 및 연결
   - 생성 방식: 참고 이미지 기반, 영우 주인공풍 두꺼운 외곽선 세미픽셀 래스터 에셋
   - 모든 에셋은 초록 크로마키 배경으로 생성 후 투명 PNG로 변환
   - 좌우 전환용으로 `left/right` 개별 파일 분리
   - 오른쪽을 보는 파일은 모두 `_right.png`
   - 홈 화면 차고/보트 슬롯에 오른쪽 방향 에셋 연결
   - 낚시 필드 화면의 차량/보트 표시를 신규 에셋으로 교체
   - 상점 차량/보트 탭 상품 아이콘을 신규 에셋으로 교체

신규 에셋 목록:

```text
assets/tivoli_sprite_sheet.png
assets/tivoli_left.png
assets/tivoli_right.png
assets/g70_sprite_sheet.png
assets/g70_left.png
assets/g70_right.png
assets/carnival_sprite_sheet.png
assets/carnival_left.png
assets/carnival_right.png
assets/valley_boat_sprite_sheet.png
assets/valley_boat_left.png
assets/valley_boat_right.png
assets/sea_boat_sprite_sheet.png
assets/sea_boat_left.png
assets/sea_boat_right.png
```

화면 연결 상태:

- 티볼리 슬롯: `assets/tivoli_right.png`
- G70 슬롯: `assets/g70_right.png`
- 카니발 슬롯: `assets/carnival_right.png`
- 밸리보트 슬롯: `assets/valley_boat_right.png`
- 바다보트 슬롯: `assets/sea_boat_right.png`
- 낚시 필드 차량 배경: 장착 차량에 따라 `tivoli_right.png`, `g70_right.png`, `carnival_right.png`
- 낚시 필드 보트: 밸리/바다 필드에서 `valley_boat_right.png`, `sea_boat_right.png`
- 상점 차량/보트 탭: 각 상품 아이콘에 동일한 오른쪽 방향 에셋 사용

주의:

- 현재 저장 상태에 따라 미보유 차량/보트는 잠김 슬롯이라 이미지가 안 보이는 것이 정상
- 보유/이벤트 획득 후 `owned` 상태가 되면 연결된 에셋이 표시됨
- 크로마키 원본 파일도 `assets/*_chroma.png` 형태로 일부 남아 있을 수 있으나, 실제 사용 파일은 투명 PNG인 `*_left.png`, `*_right.png`, `*_sprite_sheet.png`
- 차량/보트 에셋은 현재 홈 슬롯, 낚시 필드, 상점 상품 아이콘에 연결됨

### 내일 작업 시작 추천

회사에서 이어서 작업할 때는 아래 순서가 좋다.

1. `game_home.html`, `shop_screen.html`, `fishing_screen.html`을 열고 신규 차량/보트 에셋 크기 확인
   - 미보유 슬롯은 정상적으로 잠김 표시
   - 테스트가 필요하면 localStorage 상태를 임시로 수정하거나 상점/이벤트 루트로 획득 상태를 만든다.

2. 필드별 배치 QA
   - 티볼리/G70/카니발: 장착 차량에 따라 필드 배경에 표시
   - 밸리보트/바다보트: 밸리/바다 필드에서 표시
   - 캐릭터, 낚싯대, UI와 겹치면 위치/크기만 미세 조정

3. 상점 상품 아이콘 QA
   - 차량 탭: 티볼리/G70/카니발
   - 보트 탭: 밸리보트/바다보트
   - 상품명/가격 텍스트를 가리지 않는지 확인

4. 에셋 톤 QA
   - 영우 기본 캐릭터와 너무 실사처럼 튀는지 확인
   - 필요하면 차량/보트만 한 번 더 저해상도/두꺼운 외곽선 방향으로 리터치

### Codex 사용량 메모

- 사용자는 Codex 사용량 때문에 Plus만으로는 장기 집중 개발이 어렵다고 판단 중
- Pro는 Plus보다 Codex 사용량이 훨씬 넉넉하므로 7일 집중 개발에는 더 현실적
- Pro도 완전 무제한은 아니며, 작업 크기/컨텍스트/출력량에 따라 사용량이 빨리 줄 수 있음
- Plus의 Codex 가치를 토큰/금액으로 정확히 환산하는 공식 고정값은 없음
- 실제 판단은 Codex Settings > Usage 또는 `/status`에서 현재 작업의 소모 속도를 확인하는 것이 가장 정확
- 사용량 절약 원칙:
  - 하루 작업 목표를 작게 쪼갬
  - 한 번에 여러 화면을 갈아엎지 않음
  - 큰 파일 전체 반복 읽기를 줄이고 HANDOFF를 최신화
  - 이미지 생성은 꼭 필요한 기준 샘플에만 사용
  - 매일 마지막에 다음 작업 체크리스트 갱신

## 9. 새 세션 시작용 프롬프트

새 계정 또는 새 세션에서 아래 문장을 첫 메시지로 사용하면 좋다.

```text
이 폴더는 “낚시왕 영우” 게임 기획/목업 작업물입니다.
먼저 HANDOFF_낚시왕_영우.md와 낚시왕_영우_GDD.md를 읽고, HTML 목업 파일들을 확인한 뒤 이어서 작업해주세요.

현재까지 만든 화면:
- index.html: 메인 화면
- game_home.html: 거점/차고 화면
- fishing_screen.html: 낚시 화면
- shop_screen.html: 상점 화면

현재 작업 방향:
- 레트로 도트풍 병맛 낚시 RPG
- 영우 기본 캐릭터를 기준으로 전체 에셋 퀄리티를 맞춤
- 너무 고퀄 회화풍 배경은 사용하지 않음
- 데모 화면 묶음이 아니라 처음부터 끝까지 진행 가능한 출시 기반 웹게임으로 정리
- 밸런스 조정은 사용자가 직접 테스트한 뒤 마지막에 진행
- 거점은 차고/보관소 실내
- 거점에는 장비 6종, 차량 3종 고정 진열
- 보트는 거점 차고에 보관 슬롯으로 표시
- 보트는 장착/미장착이 가능하며, 미장착 상태는 `없음`으로 표시
- 상점은 제1레져
- 제1레져 파괴 후 상점주인 잠적/교환환불 불가 연출
- 상점 조건은 스포일러를 숨기고 `조건 미충족`, `Lv.2 필요`처럼 표시
- 의상 탭이 추가됨: `입문 낚시복`, `중급 낚시복`, `고급 낚시복`
- 비싼 의상일수록 고레벨 어종 출현 가중치가 증가함
- 현재 장착 의상은 거점 영우 전신, 대화창 portrait, 낚시 필드 영우 이미지, 이벤트/컷씬 영우 컷인에 반영됨
- 제1레져 낚시세트는 바다 최초 캐스팅 시 파괴
- 테무산은 획득 전까지 `??? 낚시세트`로 표시
- 낚시 캐스팅은 좌/우 방향 조준과 가까움/멀리 거리 조준의 2단계 구조
- 방향/거리 게이지는 실제 입력 타이밍 기반으로 움직이며, 버튼 입력 순간 값이 확정됩니다.
- 필드별 어종/보상 테이블, 장비별 보정치, 낚시 결과 로직은 `game_state.js`와 낚시 화면에 1차 연결됨
- `game_state.js`는 저장 상태 정규화와 이벤트 큐를 담당
- 티볼리 압수 등 주요 이벤트는 거점 화면 공통 팝업으로 표시
- 다음 작업은 화면 진입 흐름 정식화, 이벤트 팝업 전체 연결, 도감/엔딩 연결, 테스트 버튼 정리입니다.
```

## 2026-05-29 스프라이트/감정 에셋 적용 현황

- 낚시 액션 스프라이트 144장 생성 완료.
  - 원본: `assets/youngwoo_sprites/final_144/`
  - 투명 PNG: `assets/youngwoo_sprites/game_alpha/`
  - 구조: `의상 3종 x 낚시대 6종 x 포즈 8종`
  - 파일명: `youngwoo_의상_낚시대_포즈.png`
- 감정 스프라이트 72장 생성 완료.
  - 시트: `assets/youngwoo_sprites/emotion_sheets/`
  - 개별 컷: `assets/youngwoo_sprites/emotion_individual/`
  - 투명 PNG: `assets/youngwoo_sprites/emotion_alpha/`
  - 구조: `의상 3종 x 감정 24종`
  - 파일명: `youngwoo_의상_emotion_감정.png`
- 코드 적용:
  - `fishing_screen.html`: 장착 의상/낚시대/phase에 맞춰 액션 스프라이트 자동 선택.
  - `game_home.html`: 홈 대화창 portrait와 이벤트 팝업 영우 컷인에 감정 스프라이트 적용.
  - `event_scene.html`: 이벤트 연출 템플릿에서 감정 스프라이트 표시.
- 확인 결과:
  - 액션 원본 144장, 액션 투명 PNG 144장.
  - 감정 시트 3장, 감정 개별 컷 72장, 감정 투명 PNG 72장.
  - 감정 파일 누락 없음.
  - `game_home.html`, `event_scene.html`, `fishing_screen.html` 인라인 스크립트 문법 검사 통과.
- 다음 PC에서 우선 테스트:
  - `http://localhost:8000/game_home.html`
  - `http://localhost:8000/fishing_screen.html`
  - `http://localhost:8000/event_scene.html`

## 2026-05-29 물고기 스프라이트 40종 추가

- `game_state.js`의 어종 40종 기준으로 물고기/해양생물 스프라이트를 생성함.
- 실제 어종 생김새를 참고하되 게임용 픽셀아트 도감 스타일로 통일함.
- 저장 위치:
  - 시트 원본: `assets/fish_sprites/sheets/`
  - 개별 크로마키 컷: `assets/fish_sprites/individual_chroma/`
  - 게임용 투명 PNG: `assets/fish_sprites/alpha/`
  - 미획득 표시용 실루엣 PNG: `assets/fish_sprites/silhouette/`
  - 매니페스트 JSON: `assets/fish_sprites/fish_sprite_manifest.json`
  - 브라우저용 매니페스트 JS: `fish_sprite_manifest.js`
- 생성 수량:
  - 시트 4장
  - 개별 크로마키 40장
  - 투명 PNG 40장
  - 실루엣 PNG 40장
- 코드 적용:
  - `fishdex_screen.html`
    - 도감 목록/상세에 실제 물고기 PNG 표시
    - 미획득 어종은 실루엣 PNG 표시
  - `fishing_screen.html`
    - 필드에서 헤엄치는 어종이 기존 타원 실루엣 대신 생성된 물고기 스프라이트를 사용
    - 포획 결과에서 영우가 든 물고기도 해당 스프라이트로 표시
- 검증:
  - `fish_sprite_manifest.js`, `fishdex_screen.html`, `fishing_screen.html` 스크립트 문법 검사 통과
  - `alpha` 40장, `silhouette` 40장, 매니페스트 40종 확인

## 2026-05-29 작업 중단 시점 중요 이슈

다른 PC에서 이어받을 때 가장 먼저 확인해야 할 내용입니다.

### 중급/고급 낚시복 액션 이미지 문제

- `assets/youngwoo_sprites/final_144/` 기준 현재 파일 수는 144장입니다.
- 그중:
  - `youngwoo_mid_*.png` 48장
  - `youngwoo_premium_*.png` 48장
- 사용자 확인 결과, **중급 낚시복 48장과 고급 낚시복 48장 모두 이미지가 잘리는 문제가 발생**했습니다.
- 원인:
  - 중급/고급 세트는 후반 작업에서 `4 x 2` 시트 생성 후 셀 단위로 자동 분할했습니다.
  - 이 과정에서 캐릭터/낚싯대가 셀 경계에 닿거나 잘린 이미지가 섞였습니다.
- 다음 작업:
  - 중급/고급 낚시복 액션 이미지는 기존 파일을 그대로 쓰지 말고 재생성해야 합니다.
  - 안전하게 하려면 `beginner + ali`처럼 **한 장씩 생성**하거나, 시트를 쓰더라도 각 셀 안에 훨씬 큰 여백을 강제해야 합니다.
  - 재생성 대상:
    - `youngwoo_mid_*` 48장
    - `youngwoo_premium_*` 48장
  - 초심자복 `youngwoo_beginner_*` 48장은 현재 문제 제보 없음.

### 백업 이동 시도 상태

- 고급 낚시복 잘림 확인 후 `assets/youngwoo_sprites/rejected_premium_cropped_2026-05-29/` 폴더를 만들었습니다.
- 하지만 PowerShell `Join-Path` 인자 오류로 실제 파일 이동은 되지 않았습니다.
- 현재 확인 결과:
  - `rejected_premium_cropped_2026-05-29/` 폴더 내부 파일 수는 0개
  - `final_144`의 고급 낚시복 48장과 `game_alpha`의 고급 낚시복 48장은 그대로 남아 있습니다.
- 다음 PC에서 작업할 때 이 폴더는 비어 있는 임시 실패 흔적으로 보면 됩니다.

### 영우 감정표현 이미지 문제

- 현재 감정 스프라이트는 `assets/youngwoo_sprites/emotion_sheets/`, `emotion_individual/`, `emotion_alpha/`에 있습니다.
- 현재 구성은 의상 3종 x 감정 24종 = 72장입니다.
- 문제:
  - 사용자는 감정표현도 **전신 이미지**가 필요하다고 정정했습니다.
  - 현재 감정표현은 컷씬/대화창용 상반신 또는 bust 이미지 중심이라 요구와 다릅니다.
- 다음 작업:
  - 감정표현 72장은 참고용/폐기 후보로 두고, 전신 감정 스프라이트로 다시 제작해야 합니다.
  - 새 기준:
    - 의상 3종
    - 감정 24종
    - 전신 캐릭터
    - 배경은 #00ff00 크로마키 또는 투명 변환 가능 형태
    - 팔 패치/브랜드 로고 없음
  - 기존 `game_home.html`, `event_scene.html`에는 현재 감정 PNG가 연결되어 있으므로, 새 전신 감정 PNG를 만들면 파일명 규칙을 유지하거나 코드 매핑을 함께 수정해야 합니다.

### 현재 유지해도 되는 작업

- 물고기 40종 스프라이트:
  - `assets/fish_sprites/alpha/` 40장
  - `assets/fish_sprites/silhouette/` 40장
  - `fish_sprite_manifest.js`
  - `fishdex_screen.html`, `fishing_screen.html` 연결 완료
- 실루엣 40종은 물고기 투명 PNG 기준으로 다시 생성 완료.

---

## 2026-05-29 최신 저장 - 필드 배경/낚시 화면 에셋 적용 현황

저장 시각: 2026-05-29 17:09
작업 폴더:
C:\Users\PC\Documents\Codex\2026-05-26\1-rpg-1-40-2-10\1-rpg-1-40-2-10

### 오늘 확정/적용된 큰 변경

1. 필드 배경 원화 4종 생성 및 낚시 화면 적용
   - ssets/field_backgrounds/field_fresh_concept.png
   - ssets/field_backgrounds/field_valley_concept.png
   - ssets/field_backgrounds/field_sea_concept.png
   - ssets/field_backgrounds/field_ocean_concept.png
   - 검수용:
     - ssets/field_backgrounds/field_background_concept_review.png
     - ssets/field_backgrounds/field_background_game_layer_preview.png

2. 물 표현 적용 방식
   - 배경 원화는 고정으로 깔고, 물 영역에는 기존 물 타일/오버레이를 낮은 투명도로 얹음.
   - 물고기 실루엣 위에 물결 오버레이가 지나가도록 레이어 순서 유지.
   - 물 흐름 속도는 이전보다 느리게 조정 완료.

3. 물고기 실루엣 7종 공통 에셋 적용
   - ssets/fish_silhouettes/shadow_small.png
   - ssets/fish_silhouettes/shadow_long.png
   - ssets/fish_silhouettes/shadow_flat.png
   - ssets/fish_silhouettes/shadow_round.png
   - ssets/fish_silhouettes/shadow_predator.png
   - ssets/fish_silhouettes/shadow_giant.png
   - ssets/fish_silhouettes/shadow_junk.png
   - 실제 물고기 이미지는 결과/도감 쪽에서만 노출하고, 필드 이동 중에는 공통 실루엣만 사용.

4. 민물 기본 필드 연출 변경
   - 민물 나무발판 제거.
   - 영우는 강가 육지에 서 있는 방향으로 위치 조정.
   - 발밑에 약한 그림자/풀 터치를 추가해 접지감 보강.

5. 바다/보트 필드 연출
   - 바다 근해는 방파제 발판 유지.
   - 밸리보트/대양보트 필드는 차량 숨김.
   - 보트 탑승 시 영우 하반신/발은 보트에 가려지는 방식으로 처리.

6. 차량 위치/스케일 조정
   - 민물 필드 티볼리/차량이 하늘에 떠 보이던 문제 수정.
   - 민물 차량 좌표는 현재 x: W - 292, y: 222, width: 116, height: 56.
   - 바다 근해 차량은 x: W - 270, y: 178, width: 124, height: 60.
   - 집에서 검수 시 티볼리가 여전히 뜨면 민물 차량 y를 230~238 근처로 더 내리면 됨.

7. 영우 캐릭터 스케일 조정
   - 배경 나무/육지와 비교해 너무 커 보여서 필드별 높이를 줄임.
   - 현재 playerSpriteHeight():
     - fresh: 138
     - sea: 152
     - valley/ocean: 150
   - 영우가 여전히 크면 fresh 128~132, sea 142~148 정도로 추가 조정 추천.

8. 물고기 이동 범위 제한
   - 물고기 실루엣이 육지까지 침범하지 않도록 waterPlayBounds() 추가.
   - 스폰/이동/후킹/파이팅 중 모두 물 영역 안에 머물도록 clamp 처리.
   - 현재 민물 물고기 범위:
     - minX 430
     - maxX W - 118
     - minY waterLine + 108
     - maxY H - 66
   - 집에서 볼 때 물고기가 너무 오른쪽에만 있으면 minX를 390~410으로 완화 가능. 단, 육지 침범 여부 확인 필요.

9. 낚시줄 위치 보정
   - youngwoo_sprite_manifest.js의 casting_ready 낚싯대 끝 좌표 보정.
   - 이미지 로딩 전에도 manifest size/tip으로 낚싯대 끝 계산하도록 수정.
   - 낚시 전/캐스팅 자세에서 낚시줄 시작점이 낚싯대 끝에 붙도록 개선.

10. 의상 아이콘 새 생성본 적용
   - 기존 팔 패치 제거 시도는 폐기하고 새 아이콘 3종으로 교체.
   - 적용 파일:
     - ssets/outfit_beginner_icon.png
     - ssets/outfit_mid_icon.png
     - ssets/outfit_premium_icon.png
   - 백업:
     - ssets/outfit_icon_before_regen_backup/
   - 검수:
     - ssets/outfit_icon_regen_preview.png

### 현재 핵심 수정 파일

- ishing_screen.html
  - 필드 배경 원화 연결
  - 물 레이어/실루엣 레이어/발판/차량/영우 좌표 조정
  - 물고기 이동 범위 제한
  - 보트 필드 차량 숨김
- youngwoo_sprite_manifest.js
  - casting_ready 낚싯대 끝 좌표 보정
- game_home.html
  - 의상/장비 레벨 불일치 표시 및 장착 제한 반영됨
- game_state.js
  - 의상 레벨과 장비 레벨 매칭 제한 반영됨

### 집에서 먼저 검수할 것

1. ishing_screen.html 민물 기본
   - 티볼리가 육지에 붙어 보이는지
   - 영우 크기가 나무/배경과 자연스러운지
   - 물고기 실루엣이 육지로 침범하지 않는지
   - 낚시줄이 낚싯대 끝에서 시작하는지

2. ishing_screen.html 바다 근해
   - 방파제 발판과 영우 위치가 자연스러운지
   - 차량이 너무 크거나 떠 보이지 않는지
   - 물고기 실루엣이 방파제/육지 쪽으로 올라오지 않는지

3. ishing_screen.html 밸리보트/대양보트
   - 차량이 안 보이는지
   - 보트가 영우 하반신을 자연스럽게 가리는지
   - 배경 원화와 물 오버레이가 어울리는지

### 다음 작업 추천 순서

1. 낚시 화면 4필드 눈검수 후 좌표 미세 조정
   - 영우 크기/위치
   - 차량 위치/크기
   - 물고기 범위
   - 물 오버레이 농도
2. 필드 배경을 실제 게임 플로우에서 반복 플레이하며 UI 가림 여부 확인
3. 창고/상점 화면에서 새 의상 아이콘 검수
4. 낚시 화면이 안정되면 다음 단계로 도감/상점/이벤트 컷씬 쪽 에셋 톤 맞추기

### 검증 완료

- ishing_screen.html 인라인 스크립트 문법 체크 통과.
- 에셋 경로 누락 체크 통과.
- game_home.html 스크립트 문법 체크 통과.

---

## 2026-05-29 최신 저장: 배경/물/실루엣/필드 배치 적용 상태

### 작업 위치
`C:\Users\PC\Documents\Codex\2026-05-26\1-rpg-1-40-2-10\1-rpg-1-40-2-10`

### 오늘 핵심 적용
- 옷 아이콘 3종을 팔 패치/로고/엠블럼이 없는 새 에셋으로 교체했다.
  - `assets/outfit_beginner_icon.png`
  - `assets/outfit_mid_icon.png`
  - `assets/outfit_premium_icon.png`
  - 검수용: `assets/outfit_icon_regen_preview.png`
  - 백업: `assets/outfit_icon_before_regen_backup/`, `assets/outfit_icon_patch_backup/`
- 의상/장비 조합은 레벨 매칭 방식으로 확정했다.
  - Lv.1 의상: `toyRod`, `firstLeisure`
  - Lv.2 의상: `aliRod`, `temuRod`
  - Lv.3 의상: `japanRod`, `legendRod`
  - 고급 의상에 Lv.1 장비를 장착하는 식의 교차 조합은 막는다.
- 영우 스프라이트는 전체 144장 조합이 아니라 레벨 매칭 48장 기준으로 적용 중이다.
  - 시트: `assets/youngwoo_sprites/level_matched_sheets/`
  - 투명 PNG: `assets/youngwoo_sprites/level_matched_alpha/`
  - 매니페스트: `youngwoo_sprite_manifest.js`

### 현재 낚시 화면 상태
- `fishing_screen.html`에 필드별 배경 에셋을 적용했다.
  - 민물 기본: `assets/field_backgrounds/field_fresh_concept.png`
  - 계곡/보트: `assets/field_backgrounds/field_valley_concept.png`
  - 바다 근해: `assets/field_backgrounds/field_sea_concept.png`
  - 먼바다/보트: `assets/field_backgrounds/field_ocean_concept.png`
- 보트 탑승 필드(`valley`, `ocean`)에서는 차량을 표시하지 않는다.
- 민물 기본 필드는 발판을 제거하고 영우가 육지에 서는 배치로 변경했다.
- 바다 근해 필드는 방파제 발판을 유지한다.
- 보트 필드는 기존처럼 보트 위에 탑승한 느낌을 유지하며, 하반신 일부가 보트에 가려질 수 있도록 그리는 구조다.

### 물/실루엣 적용
- 필드별 물 베이스/오버레이 에셋을 추가했다.
  - `assets/water/water_fresh_base.png`, `water_fresh_overlay.png`
  - `assets/water/water_valley_base.png`, `water_valley_overlay.png`
  - `assets/water/water_sea_base.png`, `water_sea_overlay.png`
  - `assets/water/water_ocean_base.png`, `water_ocean_overlay.png`
  - 검수용: `assets/water/water_review_preview.png`
- 물 오버레이는 물고기 실루엣 위에 한 번 더 그려서, 실루엣이 물속에 있는 느낌을 주도록 했다.
- 실제 물고기 이미지 대신 낚기 전에는 공통 실루엣 에셋을 사용한다.
  - `assets/fish_silhouettes/shadow_small.png`
  - `shadow_long.png`, `shadow_flat.png`, `shadow_round.png`, `shadow_predator.png`, `shadow_giant.png`, `shadow_junk.png`
  - 검수용: `assets/fish_silhouettes/shadow_review_preview.png`
- 낚은 뒤 결과/보유 연출에서는 실제 물고기 스프라이트를 계속 사용한다.

### 최근 좌표/스케일 조정
- 영우 캐릭터가 배경 나무에 비해 너무 커 보여서 필드별 표시 크기를 줄였다.
  - 민물 기본: 약 138px 높이
  - 바다 근해: 약 152px 높이
  - 보트 필드: 약 150px 높이
- 민물 티볼리는 멀리 있는 차처럼 보이도록 크기를 줄였고, 사용자 피드백에 따라 아래로 두 차례 내렸다.
  - 현재 민물 차량 기준: `x = W - 292`, `y = 222`, `width = 116`, `height = 56`
- 물고기 실루엣이 육지까지 침범하지 않도록 필드별 물 영역 이동 범위를 제한했다.
  - 캐스팅 타깃, 낚시 중 타깃, 수영 실루엣 모두 같은 물 영역 기준으로 제한한다.
- 낚시줄은 `youngwoo_sprite_manifest.js`의 `tip` 좌표를 기준으로 낚시대 끝에 붙도록 조정했다.

### 검수 필요
- 민물 기본 필드에서 티볼리 바퀴가 육지에 자연스럽게 닿는지 최종 확인.
- 영우 크기와 발 위치가 배경 스케일에 맞는지 확인.
- 물고기 실루엣이 육지/풀/차 쪽으로 침범하지 않는지 확인.
- 물 흐름 속도와 농도가 배경을 너무 가리지 않는지 확인.
- 보트 필드에서 차량이 숨겨지고 보트 탑승 느낌이 유지되는지 확인.
- 낚시 전/캐스팅 준비/캐스팅 후 자세에서 낚시줄 시작점이 낚시대 끝과 맞는지 다시 눈검수.

### 다음 추천 작업
1. 브라우저에서 `fresh`, `sea`, `valley`, `ocean` 4필드를 실제 화면으로 눈검수한다.
2. 티볼리/영우/물고기 범위를 픽셀 단위로 마지막 미세 조정한다.
3. 상점/메인/도감 화면도 새 옷 아이콘과 필드 톤에 맞춰 통일감을 확인한다.
4. 이후 데모 프로그램 구조 정리 단계로 넘어간다.

### 검증 상태
- 최근 `fishing_screen.html` 인라인 스크립트 문법 검사는 통과했다.
- 최근 에셋 참조 점검에서 필수 이미지 경로 누락은 확인되지 않았다.

---

## 2026-05-30 최신 저장: 홈 화면 창고 배경/정면 차량/슬롯 정렬

### 작업 위치
`C:\Users\Kruz\Documents\Codex\2026-05-29\1-rpg-1-40-2-10`

### 핵심 적용 파일
- `game_home.html`
- `assets/home_garage_background_layout.png`
- `assets/home_garage_background_layout_with_parking_lines.png`
- `assets/home_rear_background.png`
- `assets/tivoli_front.png`
- `assets/g70_front.png`
- `assets/carnival_front.png`
- `assets/home_layout_preview.png`
- `home_layout_preview.html`

### 홈/창고 레이아웃
- 우측 메뉴 폭을 줄이고 창고 패널 폭을 넓혔다.
  - 창고/대사창 오른쪽 기준을 `430px`에서 `354px`로 조정.
  - 우측 메뉴/진행률 패널 폭을 `346px`에서 `292px`로 축소.
- 창고 상단의 별도 검은 제목 프레임과 `영우의 창고` 텍스트는 제거했다.
- 슬롯은 진한 박스/점선 느낌을 줄이고, 배경에 어울리는 반투명 전시 프레임으로 통일했다.
- 장착 상태는 얇은 골드 라인과 약한 강조로 표시한다.

### 창고 배경
- `assets/home_garage_background_layout.png`를 창고 패널 배경으로 사용한다.
- 주차선이 있던 원본은 `assets/home_garage_background_layout_with_parking_lines.png`로 보관했다.
- 현재 적용본은 원본 벽/조명/수납장 분위기를 유지하되, 바닥 주차선과 타일처럼 보이던 강한 선을 제거한 평평한 바닥 버전이다.
- 제일 뒷배경 후보로 `assets/home_rear_background.png`를 생성했다.
  - 현대 아파트 지하 복도/주차장 느낌.
  - 홈 화면 창고 패널 뒤에 깔 저디테일 배경으로 사용 가능.

### 차량 정면 에셋
- 창고의 차량 3종은 정면 전시 에셋을 사용한다.
  - 티볼리: `assets/tivoli_front.png`
  - G70: `assets/g70_front.png`
  - 카니발: `assets/carnival_front.png`
- 번호판 영역은 구현하지 않았고, 투명 PNG로 저장했다.
- 기존 `*_right.png` 차량 에셋은 필드/상점 등 측면 연출이 필요한 화면에서 계속 사용할 수 있다.
- 창고 차량 슬롯은 정면 에셋이 프레임 중앙에 오도록 조정했다.
  - 차량 이미지는 하단 배수구 라인과 닿지 않도록 위쪽 배치.
  - 현재 차량 에셋 크기 기준은 `118px x 95px`.

### 슬롯 정렬/크기
- 낚시대 6종:
  - 프레임 크기는 유지.
  - 에셋 크기는 `86px x 64px`로 통일.
  - 일본산 낚시대만 따로 다르게 보이던 보정을 같은 크기로 통일.
  - 낚시대 묶음은 왼쪽 벽 패널 기준으로 너무 좌측에 붙지 않도록 오른쪽으로 이동.
- 의상 3종:
  - 에셋 크기는 `58px x 70px`.
  - 프레임 안에서 중앙정렬.
  - 오른쪽 장식 벽을 침범하지 않도록 의상 슬롯 묶음을 왼쪽으로 조정.
- 차량 3종:
  - 정면 에셋 적용.
  - 프레임 중앙 기준으로 배치.
- 보트 2종:
  - 현재는 기존 측면 에셋 `valley_boat_right.png`, `sea_boat_right.png`를 사용.
  - 이후 정면 계열로 다시 만들 경우 완전 정면보다 정면 3/4 또는 약한 탑뷰 구도를 권장.

### 프리뷰/검수
- 전체 에셋 배치 확인용 프리뷰 파일을 생성했다.
  - `home_layout_preview.html`
  - 캡처: `assets/home_layout_preview.png`
- 브라우저 확인 URL:
  - `http://127.0.0.1:8765/home_layout_preview.html`
  - `http://127.0.0.1:8765/game_home.html`
- 최근 `game_home.html` 인라인 스크립트 문법 검사는 통과했다.

### 남은 확인 포인트
- 보트 2종이 차량보다 크게 느껴지는지 확인.
- 보트 정면/3/4 에셋을 새로 만들 경우 창고 바닥 배치와 크기 재조정 필요.
- `assets/home_rear_background.png`를 실제 홈 화면 최후면에 적용할지 결정 필요.

---

## 2026-06-02 최신 저장: 홈 화면 현대식 창고 배치 튜닝

### 작업 위치
`C:\Users\Kruz\Documents\Codex\2026-06-01`

### 핵심 적용 파일
- `game_home.html`
- `assets/home_garage_modern_bright_v5.png`

### 홈 배경/프레임
- 홈 화면 배경은 밝은 현대식 창고 이미지 `assets/home_garage_modern_bright_v5.png` 기준으로 맞췄다.
- 배경 앞에 CSS로 그리던 검정 창고 테두리는 제거했고, 장비 슬롯의 기본 검정 박스도 투명화했다.
- 장착 상태만 노란 테두리로 표시한다.

### 현재 홈 장비 배치 기준
- 낚시대 묶음:
  - `.rod-slots`: `left: 133px`, `top: 3px`
  - 3열 2행, 칸 폭 `72px`, 상하 gap `2px`, 좌우 gap `0`
  - 민물 낚시대 3종은 `translateY(3px)`로 3px 아래 보정.
  - 민물 낚시대 장착 시 노란 테두리가 안 보이던 문제를 막기 위해 `.rod-slot.equipped::after` 전용 오버레이 테두리를 추가했다.
- 차량 묶음:
  - `.vehicle-bays`: `left: -21px`, `bottom: 90px`
  - 차량 프레임 `166px x 128px`, 차량 간격 `23px`
  - 차량 이미지는 기본 `193px x 155px`
  - 티볼리 이미지는 `top: calc(50% + 8px)`
  - G70 이미지는 `184px x 148px`, `top: calc(50% + 3px)`
- 보트 묶음:
  - `.boat-slots`: `right: -314px`, `top: 278px`
  - 민물보트 프레임 폭 `235px`
  - 민물보트 이미지는 `top: 3px`, `width: calc((100% - 6px) * 1.21)`, `height: 77px`
  - 바다보트 슬롯은 별도 absolute 배치: `right: 246px`, `top: -46px`, `width: 300px`, `height: 126px`
  - 바다보트 이미지는 `width: calc((100% + 28px) * 1.32)`, `height: 98px`
- 의상 묶음:
  - `.outfit-slots`: `right: 38px`, `top: 57px`
  - 의상 프레임 `72px x 90px`, 간격 `1px`
  - 의상 이미지는 `75px x 91px`
  - 중급 낚시복은 `left: calc(50% - 1px)`
  - 고급 낚시복은 프레임 중앙 기준으로 복구했고, 잘림 방지를 위해 `#premiumOutfitSlot { overflow: visible; }` 유지.

### 우측 메뉴
- 우측 메뉴 묶음은 `right: 28px`, `top: 94px`, `width: 252px`.
- 상하폭은 줄이지 않고 좌우폭만 줄인 상태다.

### 주의
- 홈 화면 좌표는 배경 이미지의 선반/벽걸이 위치에 맞춰 미세 조정한 상태다.
- 다음 배치 수정 시 전체 레이아웃을 다시 흔들지 말고, 요청받은 장비군 또는 개별 에셋의 CSS 값만 조정하는 것이 안전하다.



